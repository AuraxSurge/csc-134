//First and Last names Assignment.cpp


#include "stdafx.h"
#include <iostream>
#include <string>
#include <fstream>

using namespace std;

int main()
{	
	//Declaring variable
	string fileReadInNames, firstName, lastName;
	
	//Opening the file with our names
	infile.open("LineUp.txt");
	
	//Use a while loop together with the getline() function to read the file line by line
	while (getline (MyReadFile, fileReadInNames)) {
		// Output the text from the file
		cout << fileNames;
	}
	
}